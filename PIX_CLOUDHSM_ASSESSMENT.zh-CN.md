# Pix CloudHSM 代理 —— 验证评估

> **English:** [`PIX_CLOUDHSM_ASSESSMENT.md`](PIX_CLOUDHSM_ASSESSMENT.md)
>
> **这是译文。** 权威副本是 [`PIX_CLOUDHSM_ASSESSMENT.md`](PIX_CLOUDHSM_ASSESSMENT.md)——发现错误请改那一份。CI 的 `doc-parity` 作业会在两份文件的标题结构、任何命令输出、或任何测量值与成本数字出现分歧时让构建失败。

面向对象：需要就如何使用 AWS CloudHSM 对 Pix 报文进行签名与传输做出决策的决策者。
本文档并非工程交接文档 —— `CLOUDHSM_BCB_V2_HANDOFF.md` 才是，而
`README-CloudHSM.md` 是架构说明。本文档说明哪些内容已**在真实硬件上验证**、哪些仍然**未验证**、以及本仓库刻意**不做**哪些事情。

下文每一项都标注为 MEASURED（已执行某条命令并读取其输出，即实测）或 INFERRED（由实测结果推断出的结论）。凡是无法取得证据之处，均如实说明为空缺，而不用一个貌似合理的答案填补。

---

## 1. 本仓库是什么，不是什么

它是一个面向两件事的**教学骨架**：用一把永不离开 FIPS 验证 HSM 的密钥对 Pix XML 进行签名，并在不改动报文的前提下通过双向 TLS 将结果传输给 BCB。它的设计原则是透明 —— 该代理不解读 Pix 业务内容。

它**不是一个 PSP**。第 6 节列出了真实参与方必须在其之上构建的内容，以免把这项工作的范围误认为已具备交易就绪能力。

---

## 2. 在真实 CloudHSM 硬件上验证

创建、驱动并销毁了两个一次性集群。此前识别出八个阻碍架构决策的问题；这八个现在都有了答案。

| # | 问题 | 结论 | 证据 |
|---|---|---|---|
| 1 | 签名密钥能否创建为不可导出？ | **可以，但必须显式指定** | 实测 `extractable: false, never-extractable: true, always-sensitive: true, local: true`。SDK 5 默认为 `extractable=true`；必须显式传入该属性。 |
| 2 | JCE provider 会否暴露私钥字节？ | **不会** | 实测 `CloudHsmRsaPrivateCrtKey`，`getEncoded()=null`，`getFormat()=null`。 |
| 3 | HSM 审计日志能否证明某次签名发生在其内部？ | **不能 —— 证据无法取得** | 实测：在约 10 次私钥操作之后，审计日志中**根本没有任何签名操作码**。见 §4。 |
| 4 | RSA 签名是否端到端可用？ | **是** | 实测 256 字节 RSA-2048 签名，`Verified OK`；CSR 验证通过。 |
| 5 | 使用 HSM 持有的客户端密钥能否完成双向 TLS？ | **能** | 实测服务端记录 `depth=0 C=BR, O=pixpoc, CN=pix-client` 与 `1 server accepts that finished`。 |
| 6 | 是否协商出 BCB 强制要求的最低加密套件？ | **是** | 实测双方均为 `Cipher is ECDHE-RSA-AES128-GCM-SHA256`。 |
| 7 | TLS 1.3 是否可用？ | **可用** | 实测 `Protocol: TLSv1.3`，`TLS_AES_256_GCM_SHA384`。 |
| 8 | 现有签名代码能否在 SDK 5 上不加修改即可运行？ | **能** | 实测使用一把 `never-extractable` 密钥得到 `SIGN_OK=true, length=1729, VERIFY=true`。 |

### 为什么问题 8 才是界定此次迁移范围的关键

`XmlSigner` 在 SDK 5 上**不加修改**即可运行，因为 JSR-105 会把 `Signature` 操作路由到
CloudHSM provider。因此从 SDK 3 → SDK 5 的迁移**并非**对签名逻辑的重写。
全部难度都落在 TLS 那一半：Netty 想要密钥*字节*，而 HSM 只会给你一个*句柄*。

---

## 3. 推荐的传输路径，及其理由

考虑了四种向 Netty 提供 HSM 持有客户端密钥的方式。**路径 D —— 一个把 HSM 的 `PrivateKey`
对象交给使用 `SslProvider.JDK` 的 SSL 上下文的 Java 客户端** 是推荐方案。

决定性证据是问题 2。由于 `getEncoded()` 返回 `null`，任何需要密钥字节的路径都不只是笨拙 ——
而是**不可能**，这不是投入多少工作量的问题：

- **OpenSSL engine 路径：被实测否证。** `-keyform engine` 以
  `ENGINE_load_private_key: no load function` 失败。该 engine **未实现任何 load function**，因此
  唯一的机制是一个 `reference-pem` 文件。nginx 的 `ssl_certificate_key engine:...`
  配置永远不可能奏效。这曾是一个真正的未知，如今已经关闭。
- **路径 B（`SslProvider.OPENSSL` 搭配 HSM 密钥）：需改用 `SslProvider.JDK`**，原因相同 ——
  原生 provider 需要字节。
- **路径 D 可行**，因为 JSSE 接受一把其操作被委派给某个 provider 的 `PrivateKey`，而从不读取其密钥材料。

路径 D 的代价，直说：它放弃了原生 OpenSSL engine，因此 TLS 吞吐是 JSSE 的水平而非
tcnative 的水平。对 Pix 的报文速率而言这是值得的取舍；对另一种工作负载则未必。

---

## 4. 证据未能触及之处

**运营方无法从审计日志证明某个给定的签名是在 HSM 内部产生的。** 这一点原本预期可证明，实则不能。
实测：审计日志记录了 `CN_GENERATE_KEY_PAIR`×1、`CN_LOGIN`×29、`CN_CREATE_USER`×3、
`CN_APP_FINALIZE`×19、`CN_ENCRYPT_SESSION_V2`×53 —— 而**没有任何签名操作码**，尽管确实发生了
大约十次私钥操作。CloudHSM 审计的是**管理**命令，而非数据面密码运算。

替代论证是演绎性的，且比一行日志更有力：该密钥为 `local: true`（以记录在案的
`CN_GENERATE_KEY_PAIR` 为锚，故它是在 HSM 内生成的），为 `never-extractable: true`（故它
从未离开过），且一个签名可用其公钥验证通过。一个验证通过的签名，由一把诞生于 HSM 内部且无法
离开的密钥产生，就**必定**是在 HSM 内部计算的。不存在其他可能。

这对 homologação 很重要：若 BCB 要求逐笔交易的密码学审计证据，**HSM 日志将无法提供**，
答案只能是上述演绎推断加上应用层审计记录。

---

## 5. 高可用：改变容量规划的那个发现

构建了一个跨两个可用区的双 HSM 集群，在持续签名的同时删除其中一个 HSM，随后又将该 HSM 恢复。
结果与"两个 HSM 分布在两个 AZ = 高可用"这一直觉解读相矛盾。

| 条件 | 签名 | 延迟 |
|---|---|---|
| 2 个 HSM，启用密钥可用性检查 | **成功** | 0.39–0.52 s（实测） |
| 1 个 HSM（删除其一），启用检查 | 对全新进程客户端**完全失败**；对长期存活的会话则未验证 | 报错前耗时 87.2 s（实测 ×3，散布 0.04 s） |
| 恢复第 2 个 HSM | **自动恢复** | 0.45 s（实测） |

错误信息很明确：`Cannot perform the requested key operation as the key must be
available on at least 2 HSMs`。

> **实测：长期存活的 JVM 会话不会被中断，这一点对生产环境否证了上面那一行。** 一个专门构建的双 HSM
> 集群，quorum 处于其**默认（启用）**状态，密钥在健康时生成（`cluster-coverage: full`，
> `never-extractable: true`）。一个 JVM 只安装一次 CloudHSM JCE provider、只加载一次密钥库、
> 只解析一次 `PrivateKey` 句柄 —— 然后在循环中签名。运行途中删除了一个 HSM。结果：**279 次签名，
> 0 次失败**，在删除之后仍持续了 **475 秒**，全程每次 **1.9–2.2 ms**。
>
> 因此 quorum 只在客户端**建立**其会话时、以及在创建或列举密钥时才被强制执行 —— 而不是在针对已解析
> 句柄的每一次私钥操作上执行。AWS 的故障排查页面指向同一结论：它把密钥生成、`key list`、以及
> *"启动了一个新的 SDK 实例"* 列为触发条件，并指出 *"OpenSSL 频繁 fork 出新的 SDK 实例"*。
>
> **为什么早先的测量给出了相反的结论。** 每一次配置 C 的失败都是用 `cloudhsm-cli` 测得的，它
> **每次调用都启动一个全新进程** —— 因此每次尝试都是一个新的 SDK 实例，而这本身就是一个触发条件。
> 那些 87.2 s 的失败是真实的，也正是全新进程客户端所看到的；它们**不是**长期存活的代理会看到的。
>
> **这改变了什么，又没有改变什么。** 配置 C 并非上面所述的瞬时全面中断：一个运行中的代理会在一个
> 幸存的 HSM 上继续签名。它仍然是应当避免的配置，其理由依然是实测得来的 —— 在降级窗口期内的一次
> **重启**无法重新建立会话，因此任何部署、崩溃、扩容或容器替换都会把降级转变为中断，且密钥的创建与
> 轮换在此期间全程失败。规划三个 HSM 仍然是 AWS 自己有文档记载的建议。区别在于该失败是**由重启触发
> 而非即时发生**，这把事故形态从"Pix 现在就停"改变为"Pix 会在下一次重启时停" —— 而后者很容易被
> 误认为已经挺过了这次故障。


**因此两个 HSM 并不是一个冗余配置 —— 它只是那个检查得以通过的最低门槛。**
失去其中一个就会使集群跌破 quorum，签名彻底停止。要在保持检查启用的同时容忍失去一个 HSM，
就**需要三个 HSM**。

### 三种配置，其中之一被严格劣汰

本文档的一个早先草稿曾说 `--disable-key-availability-check` "不应在生产中使用"。那种说法过强，
在此更正。该标志把关的是**使用**，而非复制：AWS 将 SDK 5 中的密钥同步记载为"一个全自动的过程"，
而 quorum 是一个按操作检查的独立运行时条件。因此禁用它并不会让密钥的复制程度降低 ——
它只是不再让集群拒绝使用一把它已经持有的密钥。

AWS 文档与上面的实测逐字吻合，包括那段失败文本：*"任何创建或使用令牌密钥的尝试都将失败……
该密钥必须在被使用前于至少 2 个 HSM 上可用。"* 注意其中的**创建或使用** —— quorum 会针对当前
集群状态在每次操作时重新评估，这正是为什么一把已存在、已完全复制的密钥会在集群跌至一个 HSM 的
那一刻停止工作。

| | HSM 数 | Quorum | 失去 1 个 HSM | 每月，sa-east-1 |
|---|---|---|---|---|
| **A** | 3 | 启用 | 签名继续（尚余 2 个） | $5,957 |
| **B** | 2 | 禁用 | 签名继续（实测 5/5，0.38-0.46 s） | $3,971 |
| **C** | 2 | 启用 | 运行中的会话存活；下一次重启时失败 | $3,971 |
**配置 C 的成本与 B 完全相同，但严格更差。** 无论其他方面如何决定，当前这种
"两台 HSM 加 quorum"的形态都不应作为生产环境运行的方案。

A 与 B 之间是一个真实的权衡，而非形式上的选择：

- **A 买到的是强制约束，而不仅仅是余量；它同时消除了一个竞态和一处持久性缺口。**
  集群会拒绝使用尚未复制到两台 HSM 上的密钥，因此一个复制不足的密钥不会被意外使用。
  这也消除了 AWS 明确记录在案的一个竞态：使用*新创建*密钥的调用可能被路由到一台尚未
  持有该密钥的 HSM，从而**失败**——若不这样处理，官方给出的缓解措施是在应用层重试。
  quorum 把这种随机失败转化为一次确定性的等待。这两项特性都不需要任何人靠纪律来维持。
- **B 只需要一条运维规则，而且它不是那条显而易见的规则。** 我最初把这条规则写成
  "在任何密钥创建之后核对 `cluster-coverage: full`"。实测表明这项检查**永远不会失败**：
  在集群已退化到只剩一台 HSM 时创建的密钥同样会报告 `cluster-coverage: "full"`，因为
  `full` 的含义是*存在于集群当前的每一台 HSM 上*——而当时只有一台。coverage 是相对于
  当前成员数而言的，并不是持久性度量。真正有效的规则有两个部分：**在创建或导入密钥之前，
  核实至少有两台处于 ACTIVE 状态的 HSM**——要清点 HSM 数量，而不是去读一个 coverage
  字符串——**并且在依赖一个新创建的密钥之前，先确认它可用**，因为没有 quorum 时，调用可能
  被路由到一台尚未持有该密钥的 HSM。对本工作负载而言，这两点都很容易做到：
  PSP 签名密钥和 mTLS 客户端密钥都是在开通时生成一次、仅在轮换时再生成一次，两者都是
  有计划的活动。B 所接受的暴露是：在退化窗口期间创建的密钥，外加 AWS 声明的自动备份之间
  24 小时的窗口（在诸如增删 HSM 这类集群生命周期事件发生时，还会额外进行备份）。
- **B 在应对双重故障时更为单薄。** 如果幸存的那台 HSM 在替换节点完成同步之前就失效，
  那么就只能从备份恢复。对一个静态签名密钥而言，这不会丢失任何东西；但对自上次备份以来
  创建的密钥而言，它们会丢失。

**可用区（Availability Zone）布局是选择 A 的一部分。** sa-east-1 有三个 AZ
（`sa-east-1a`、`1b`、`1c`），因此三台 HSM 可以各自独占一个 AZ。仅跨两个 AZ 部署的
三台 HSM **无法**容忍一次 AZ 故障：丢失承载其中两台的那个 AZ 后只剩一台，低于 quorum，
其停摆后果与配置 C 相同。

### 成本（按目录价）

`hsm2m.medium` 的按需目录价，取自 AWS Pricing API（服务代码 `CloudHSM`），按每月
730 小时计。仅计 HSM 费用——不含 EC2、数据传输或备份，也不含单独的非生产集群或
homologação 集群。

| 区域 | 2 台 HSM | 3 台 HSM | 第三台 HSM 的成本 |
|---|---|---|---|
| sa-east-1（圣保罗）—— $2.72/HSM/小时 | $3,971/月 · $47,654/年 | $5,957/月 · $71,482/年 | **+$1,986/月 · +$23,827/年** |
| us-east-1（北弗吉尼亚）—— $1.60/HSM/小时 | $2,336/月 · $28,032/年 | $3,504/月 · $42,048/年 | +$1,168/月 · +$14,016/年 |

考虑到 RSFN 连接性，对 Pix 而言实际可用的区域是北弗吉尼亚，而圣保罗**每台 HSM 的价格要贵
70%**。BCB 是否*要求* HSM 必须位于巴西境内，本次工作并未确证——应将区域选择视为一个
有待确认的开放问题，而非既定约束。

如需权威估算，请使用 AWS Pricing Calculator；上述数字是用于支撑选型决策的目录价，
而非报价。

### 配置 B 现已完成实测

为此专门搭建了第三个集群进行测试：两台 HSM 跨两个 AZ、quorum 关闭，在健康状态下生成一个
归属于 PSP 的密钥（`cluster-coverage: full`、`never-extractable: true`、`sign: true`），
先做一次五次签名的基线，然后删除一台 HSM。

```console
baseline, 2 HSMs   5/5 ok   0.46-0.52 s
degraded, 1 HSM    5/5 ok   0.38-0.46 s   <- the existing key still signs
```

**配置 B 的可用性主张成立**，无需更改配置、也无需重启。同样的删除操作在配置 C 下
3/3 全部失败，每次耗时 87.2 s。在退化*期间*生成的密钥同样可用——并且报告了
`cluster-coverage: "full"`，正是这一点使上文所述那种朴素的安全检查失效。

仍未实测的是配置 **A**：一个三台 HSM 的集群丢失其中一台。它可直接由 quorum 规则（仍剩
两台，因此满足 quorum）以及配置 B 已证明的"单台幸存 HSM 能以全速提供签名"这一点推导得出，
但并未实际运行过。3-AZ 布局要求同样未经实测；它是由同一条规则推导出来的。

另有三项运维层面的发现：

1. **故障是慢速的，而非快速的。** 87 秒是一个固定的内部重试预算，而不是网络抖动——
   三次测量彼此吻合到 0.04 s 以内。在同步的 Pix 请求路径上，请求会堆积，而不是快速失败。
   客户端超时必须据此设置。
2. **恢复无需更改配置。** 实测：在客户端配置中并未加入替换 HSM 的新 IP 的情况下，
   签名即已恢复，因此客户端会自行发现集群成员。请注意，替换节点会以一个**新的** ENI IP 出现。
3. **控制平面的状态滞后于数据平面。** 激活操作已返回成功，而 `describe-clusters` 在约
   80 s 内仍读出 `INITIALIZED`，且在该窗口期间 `CreateHsm` 被拒绝。删除时表现出同样的
   滞后：两台 HSM 都已报告删除，而集群在约 120 s 内仍列出 2 台。任何信任即时响应的自动化
   都会出错。

### 集群生命周期的审计覆盖被拆分在两个来源中

实测：一台替换 HSM 的审计流以 `CN_RESTORE_BEGIN`、`PARTITION_BACKUP_RESTORE_LOG`、
`CERT_AUTH/RSA/KEK` 开头——密钥复制进一台加入集群的 HSM **是**可审计的。但**移除操作
不会被记录为一个事件**：被删除 HSM 的审计流只是以 `END_MARKER_OPCODE` 结束。仅凭
CloudHSM 日志本身，一次有意的删除与一次崩溃无法区分——两者看上去都只是一条中断了的流。

生命周期信息其实存在于 **CloudTrail** 中（实测：`DeleteHsm`、`CreateHsm`×4、
`InitializeCluster`，全部有归属主体）。CloudTrail 还会记录**被拒绝的**尝试，这对取证
很有用。因此，PSP 的审计轨迹必须**同时**采集这两个来源；只采集 HSM 日志会丢失
"是谁移除了一台 HSM"。

---

## 6. 有意未实现的部分

本代码库涵盖签名、mTLS、CloudHSM 客户端/容器，以及透明 HTTP 代理。参与机构仍需自行构建
以下内容，它们均未出现、也未尝试实现：

- 付款发起，以及超出传输层的完整 DICT 记录生命周期
- 入站 SPI 异步消息处理
- 清算、对账与退款状态机
- MED 2.0 / 资金追回、欺诈标记、事件通知
- Pix Automático
- 授权、流动性管理与欺诈决策
- 运营 SLA 及其背后的容量模型

此处的缺失是范围界定的决定，而非疏漏。

---

## 7. 投产前尚未通过的闸口

按其潜在危害程度排序。

1. **集群规模。** 启用 quorum 的两台 HSM 在单台 HSM 故障时即造成完全停摆，不得投产
   （见 §5）。要么采用启用 quorum 的三台 HSM（按圣保罗目录价 +$23,827/年，且必须分处
   三个独立的 AZ），要么采用关闭 quorum 的两台 HSM，外加"绝不在退化状态下创建密钥"这条规则。
   第二种方案尚未实测。
2. **证书吊销检查出厂时处于关闭状态。** 该开关已存在并经过测试，但目前一份已被吊销的
   BCB 证书仍会被接受。启用它需要两个本代码库无法确证的事实：其一，信任参数中持有的是
   ICP-Brasil CA，而非一份固定的叶证书（若是固定的叶证书，则根本无法进行任何吊销检查）；
   其二，CRL/OCSP 端点可从 RSFN 访问到。
3. **告警已定义并经过测试，但尚未部署。** CDK 应用能够合成，其断言也在 CI 中运行；
   但没有任何 stack 被推送到某个账户中。
4. **tcnative 被固定为 `linux-x86_64-fedora`。** 由于显式请求了 `SslProvider.OPENSSL`，
   缺少该原生库的主机会在启动时失败。TLS 行为仅在该架构上验证过。
5. **三项安全手册要求依赖于一次 Internet Archive 取回。** Manual de Segurança do Pix
   v3.7 在 BCB 官网上返回 404，而同系列的其他手册返回 200（已用正向对照验证）。架构文档中
   引用的 TLS 版本、DNS-TTL 及 ICP-Brasil 证书链要求，均来自那份存档副本。应通过 BCB 的
   接入流程重新获取这些要求，而不要将其当作已成定论。
6. **逐笔交易级别的 HSM 审计证据无法获得**（见 §4）。如果 homologação 要求提供，那么答案
   就是那套演绎论证外加应用层审计，而这一点应事先达成一致，而不是在认证过程中才发现。

---

## 8. 如何核验这些结论

上文每一条结论都可追溯到一条命令及其输出。测量方法、遇到的陷阱以及负向对照都记录在
`CLOUDHSM_BCB_V2_HANDOFF.md` 和 `README-CloudHSM.md` 中。CI 中的每一条契约断言都配有一个
负向对照，并已确认它会因正确的原因而失败；CI 的验证方式是读取测试数量，而非看状态勾——
一个测试数为零却显示绿色的任务在本代码库中已经发生过一次，如今已针对该情况显式加以防护。

为本次验证而创建的所有 AWS 资源均已销毁，并确认归零。
