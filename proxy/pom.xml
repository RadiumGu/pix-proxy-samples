<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>com.amazon.aws</groupId>
    <artifactId>pix</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>

    <name>AWS PIX (Brazilian Instant Payment System)</name>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <java.version>11</java.version>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>

        <quarkus.version>1.7.0.Final</quarkus.version>
        <camel-quarkus.version>1.0.0</camel-quarkus.version>

        <aws.bom.version>2.13.0</aws.bom.version>
        <lombok.version>1.18.12</lombok.version>

        <junit.version>4.13.1</junit.version>
        <commons-io.version>2.14.0</commons-io.version>

        <!--
          Security overrides. quarkus-bom 1.7.0.Final pulls Netty 4.1.49.Final and
          jackson-databind 2.11.2, both of which carry advisories that matter here:

            Netty 4.1.49 / CVE-2021-43797 - netty-codec-http does not reject a header
            name containing a control character, so a downstream server can be made to
            see a request boundary the upstream one did not. This proxy forwards
            attacker-influenced headers to the BCB, which is exactly the shape of
            deployment the advisory is about. Fixed in 4.1.71.Final.

            jackson-databind 2.11.2 / CVE-2020-25649 - the XML factory does not disable
            external entity resolution, an XXE exposure in a process that also handles
            signed XML. Fixed in 2.11.4.

          Both are pinned well past the fix so the later advisories in each line are
          covered too, and netty-tcnative moves with Netty because the two are released
          as a matched pair.

          These BOM imports are deliberately declared BEFORE quarkus-bom: for imported
          BOMs Maven takes the FIRST declaration that manages an artifact, so putting
          them after quarkus-bom would leave the vulnerable versions in place and the
          override would silently do nothing. Verified with `dependency:list`, not by
          assuming - see README-CloudHSM.md.
        -->
        <netty.version>4.1.118.Final</netty.version>
        <netty-tcnative.version>2.0.70.Final</netty-tcnative.version>
        <jackson.version>2.15.4</jackson.version>
    </properties>

    <modules>
        <module>core</module>
        <module>cloudhsm</module>
        <module>kms</module>
        <module>test</module>
    </modules>

    <dependencyManagement>
        <dependencies>

            <!--
              MUST stay above quarkus-bom: the first imported BOM that manages an
              artifact wins, so moving these down re-introduces the CVEs.
            -->
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-bom</artifactId>
                <version>${netty.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!--
              netty-tcnative has no BOM artifact, so it is managed directly. 2.0.70.Final
              is the version Netty 4.1.118.Final itself declares in netty-parent, and the
              two are released as a matched pair - moving Netty without moving tcnative is
              how the OpenSSL provider ends up loading against the wrong JNI ABI. The
              classifier stays linux-x86_64-fedora, which is the one that carries the
              Amazon Linux-compatible native build.
            -->
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-tcnative</artifactId>
                <version>${netty-tcnative.version}</version>
                <classifier>linux-x86_64-fedora</classifier>
            </dependency>

            <dependency>
                <groupId>com.fasterxml.jackson</groupId>
                <artifactId>jackson-bom</artifactId>
                <version>${jackson.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <dependency>
                <groupId>com.amazon.aws</groupId>
                <artifactId>pix-core</artifactId>
                <version>1.0.0</version>
            </dependency>

            <dependency>
                <groupId>io.quarkus</groupId>
                <artifactId>quarkus-bom</artifactId>
                <version>${quarkus.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <dependency>
                <groupId>org.apache.camel.quarkus</groupId>
                <artifactId>camel-quarkus-bom</artifactId>
                <version>${camel-quarkus.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <dependency>
                <groupId>software.amazon.awssdk</groupId>
                <artifactId>bom</artifactId>
                <version>${aws.bom.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <dependency>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>${lombok.version}</version>
                <scope>provided</scope>
            </dependency>

            <dependency>
                <groupId>junit</groupId>
                <artifactId>junit</artifactId>
                <version>${junit.version}</version>
                <scope>test</scope>
            </dependency>

            <dependency>
                <groupId>commons-io</groupId>
                <artifactId>commons-io</artifactId>
                <version>${commons-io.version}</version>
            </dependency>

        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.6.1</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-shade-plugin</artifactId>
                    <version>3.1.1</version>
                    <configuration>
                    </configuration>
                    <executions>
                        <execution>
                            <phase>package</phase>
                            <goals>
                                <goal>shade</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>io.quarkus</groupId>
                    <artifactId>quarkus-maven-plugin</artifactId>
                    <version>${quarkus.version}</version>
                    <executions>
                        <execution>
                            <goals>
                                <goal>build</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>org.jboss.jandex</groupId>
                    <artifactId>jandex-maven-plugin</artifactId>
                    <version>1.0.7</version>
                    <executions>
                        <execution>
                            <id>make-index</id>
                            <goals>
                                <goal>jandex</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
